**Алгоритм диагностики:**

1. **DNS:** Можем ли мы вообще превратить имя `oarm-1224.nsd.ru` в IP-адрес?
2. **Ping:** Отвечает ли хост на ICMP (жив ли он)?
3. **Port:** Открыт ли порт 5985 (WinRM)?

Если хоть один шаг падает — мы возвращаем ошибку и **не идем дальше**.