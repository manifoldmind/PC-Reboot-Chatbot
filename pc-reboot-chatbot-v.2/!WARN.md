- Фильтрация ПК только по НРД
- TODO: Виртуальные машины (Virtual User Machine) и Linux (RedOS) могут иметь атрибут title без домена (например, "win10-1234" или "redos-5678").
	В версии v2.1 необходимо добавить логику:
            1. Запрашивать атрибут 'model' или 'type' (например, "Virtual User Machine").
            2. Если это ВМ или Linux, применять альтернативные правила валидации title (например, проверка по внутреннему реестру имен или суффиксам).
	Пока мы строго фильтруем только по наличию суффикса ".nsd.ru".